# Скопировать в .env, значения заменить. .env не коммитить!
APP_ENV=dev
PORT=8000
DATABASE_URL=postgresql://dev:dev@localhost:5432/vkr

# Заглушки под будущие сервисы (LLM-проверки, лаба 2+)
LLM_API_KEY=changeme
LLM_MODEL=changeme