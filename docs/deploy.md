
```markdown
# Деплой

Прод живёт с первой недели и только наращивается.

## Лаба 1: hello-world (текущий прод)

Хостинг: **Vercel** (бесплатный тариф Hobby). Выбран по критерию:
публичный URL, открывающийся без VPN у любого члена команды и проверяющего.

- Прод-URL: https://vkr-red.vercel.app/
- Что отдаёт: статическая заглушка `public/index.html` — статус «ok»

### Как обновить прод (текущий способ)

1. Изменить `public/index.html`
2. Задеплоить заново через drag-and-drop на vercel.com
   (или через подключённый к репозиторию проект — см. ниже)

### Подключение автодеплоя из репозитория (в процессе)

Vercel запросил доступ к организации GitHub vstu-sii — запрос отправлен
владельцу. После одобрения: Add New → Project → выбрать репозиторий →
Output Directory: `public` → автодеплой при каждом пуше.

## Бэкенд (FastAPI + Docker): лаба 2+

- Образ собирается и проверяется в CI (`docker build`) на каждый PR с лабы 1
- Vercel поддерживает Docker-деплой (тариф Hobby) — кандидат на прод бэкенда;
  ограничения: отдельная Postgres, лимиты на длительные задачи, cron раз в день
- Проверка рабочего варианта — задача лабы 2; альтернативы: Timeweb Cloud и др.

## Проверка живости прода

Открыть https://vkr-red.vercel.app/ — виден статус «ok» — прод жив.
```